// 메인 페이지에서 루틴을 보여주는 용도의 컴포넌트 안에 있는 아이템
import { ScrollView, Text, TouchableOpacity, View } from 'react-native'
export default function MainRoutineItem () { return (
    <View>
      <Text>1</Text>
    </View>
  )
}